import { Latex } from './Latex';export default function TheoryModal({item,onClose}:{item:any,onClose:()=>void}){return <div className="modal-backdrop" onClick={onClose}><div className="modal-card" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}>Cerrar</button><span className="badge">Fundamento teórico</span><h2>{item.title}</h2><Latex block expr={item.formula}/>{item.definitionFormula&&<Latex block expr={item.definitionFormula}/>}<h3>Definición matemática formal</h3><p className="text">{item.definition}</p><h3>Relación con la implementación</h3><p className="text">{item.implementation}</p><div className="exercise-box"><h3>Ejemplo detallado paso a paso</h3><p className="text">{item.exerciseIntro}</p>{item.exerciseFormula&&<Latex block expr={item.exerciseFormula}/>}<ol>{item.steps.map((s:string,i:number)=><li key={i}>{s}</li>)}</ol><Latex block expr={item.resultFormula}/><p className="text"><strong>Conclusión:</strong> {item.conclusion}</p></div></div></div>}