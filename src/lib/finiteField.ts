export const mod=(a:number,p:number)=>((a%p)+p)%p;export function generateVectors(q:number,k:number):number[][]{const out:number[][]=[];for(let i=0;i<q**k;i++){let v=i;const row:number[]=[];for(let j=0;j<k;j++){row.unshift(v%q);v=Math.floor(v/q)}out.push(row)}return out}export function multiplyVectorMatrix(v:number[],M:number[][],p:number):number[]{const cols=M[0].length;return Array.from({length:cols},(_,j)=>mod(v.reduce((s,x,i)=>s+x*M[i][j],0),p))}