export function hammingDistance(a:number[],b:number[]):number{return a.reduce((s,x,i)=>s+(x!==b[i]?1:0),0)}export function hammingWeight(a:number[]):number{return a.filter(x=>x!==0).length}export function minimumDistance(code:number[][]):number{let min=Infinity;for(let i=0;i<code.length;i++)for(let j=i+1;j<code.length;j++)min=Math.min(min,hammingDistance(code[i],code[j]));return min===Infinity?0:min}export function uniqueRows(rows:number[][]):number[][]{const seen=new Set<string>();const out:number[][]=[];for(const r of rows){const k=r.join(',');if(!seen.has(k)){seen.add(k);out.push(r)}}return out}